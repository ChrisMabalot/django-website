version: "3.8"

x-services-volumes: &service-volumes
  - ./:/app/:rw,cached

services:
  website:
    image: website-portfolio:latest
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes: *service-volumes
    ports:
      - "8000:8000"
